# OpenCareEyes v0.6.1 发布检查表

## 代码与自动测试

- [ ] `python -m ruff check src tests scripts` 通过。
- [ ] `python -m pytest -q` 全部通过，v0.4.1 回归测试无回归。
- [ ] `EffectCoordinator.reconcile(RuntimeIntent)` 是 filter、dimmer、breaks、focus 的唯一实际写入口；页面、托盘、热键、方案、调度、全局暂停和情境感知没有旁路。
- [ ] 组合测试覆盖应用规则抑制期间启用功能、切方案、调度切换、全局暂停叠加全屏、情境中修改偏好及退出后使用最新状态。
- [ ] 故障测试覆盖 Gamma 部分成功、HDR 切换、遮罩创建失败、QSettings sync 失败、效果补偿失败和传感器连续失效。
- [ ] `state_changed` 只在语义变化时发送，倒计时由 `break_tick` 更新；稳定 10 秒最多发布一次完整状态。
- [ ] 滑块预览最多 20Hz 合并，松手后仅成功持久化一次，主线程无可见阻塞。
- [ ] `CompanionPresentationSnapshot` 和专用展示信号更新伙伴；相同状态不重复设置尺寸、样式、显示层级或加载资源。
- [ ] `StatusPresenter` 在无 Qt 环境中稳定投影四项功能的期望/实际状态、抑制原因和恢复条件。

## schema v6 与迁移

- [ ] 空配置、v1→v2→v3→v4→v5→v6、v5→v6、幂等、备份/写入/sync 失败回滚及未来 schema 只读均已验证。
- [ ] 全新安装默认为渐进提醒并显示白鼬；天气和宠物声音默认关闭。
- [ ] v0.3 缺失 `reminder_style` 的用户迁移为 `fullscreen`，保留原提醒行为。
- [ ] 旧休息周期迁入短周期，长周期默认关闭。
- [ ] 固定时间调度保留原星期；旧日出日落调度迁移为全周。
- [ ] v4 已显示桌宠的用户升级后启用伙伴，原先隐藏的用户继续隐藏；`pet_x/pet_y` 转为相对锚点且越界位置可恢复。
- [ ] schema v6 只新增 `companion/quick_actions_json`；默认顺序、合法操作、原子保存和失败回滚均已验证。
- [ ] schema v6 的当前宠物、显示状态、大小、锚点、外观偏好、天气开关与休息场景均可无损保存。
- [ ] 护眼、休息、专注、调度、热键、日间/夜间方案及日出日落偏移在迁移后保持原值。

## 通用宠物包与桌面交互

- [ ] 白鼬官方包使用 schema v2、`asset_scale=2` 和共享 PNG atlas；每个 `source_rect` 位于真实 PNG 边界内，schema v1 兼容包仍可加载。
- [ ] 正式白鼬包与不同画布尺寸/动作映射的测试宠物包均通过；业务代码扫描不存在物种、爪子、脖子或尾巴硬编码。
- [ ] 拒绝非法 `pet_id`、绝对路径、`..`、包外符号链接、非 PNG/WAV/JSON、超大图片、缺失帧、不兼容版本和重复 JSON 键。
- [ ] 所有宠物必需动作均存在；可选动作缺失时退回 `idle` 或通用气泡，不阻止休息和快捷工具。
- [ ] 宠物切换先验证/预加载，失败时恢复原宠物；默认包损坏时显示静态占位角色和中文错误。
- [ ] 单击、拖动、松手、右键、光标靠近、光标静止、自主发呆/踱步/玩耍/睡觉和休息到点动作均已验证。
- [ ] 用户拖动才更新永久锚点；自主移动不覆盖锚点。隐藏或受安全情境抑制后动画、光标采样和自主行为定时器全部停止。
- [ ] 宠物切换期间休息到点、拖动中锁屏和减少动画动态切换不丢失休息/计时状态。

## 天气、节日、道具与快捷工具

- [ ] 天气未授权时零天气网络请求；授权提示明确说明经纬度与网络服务，Open-Meteo 请求使用 HTTPS 和 5 秒超时。
- [ ] 请求不发送城市名，日志/诊断不含坐标 URL；结果只在内存保留，30 分钟刷新和 2 小时旧数据降级已验证。
- [ ] 晴、雨、雪、雷暴、圣诞节、元宵节、写作、计算器和 EDA 外观映射及互动/手动/节日/天气/应用优先级已验证。
- [ ] 用户倒计时与休息状态机独立，不保存历史；便签最多 50 条且正文不进入 AppState、日志、备份或诊断 ZIP。
- [ ] CPU/内存只在系统状态页可见时采样，关闭后停止；回收站先显示项目数/大小并始终保留 Windows 确认。
- [ ] 眺望远方、雪球呼吸、伸展陪练和团子安睡只改变展示，不创建第二套休息计时或跳过规则。

## Windows 实机与界面

- [ ] Windows 10 与 Windows 11、非管理员权限均已启动。
- [ ] 亮色、暗色、跟随系统和高对比度无需重启即可生效。
- [ ] `ThemeSnapshot` 同步应用到主窗口、欢迎流程、宠物、气泡、休息界面和快捷工具；各窗口不单独猜测系统主题。
- [ ] 100%、125%、150% 和 200% DPI 下无裁切，纯键盘可完成核心操作，状态不只依赖颜色表达。
- [ ] 陪伴屋宽屏舞台约占 58%；内容宽度低于 640 逻辑像素时改为单列且无横向滚动。
- [ ] 单屏、负坐标双屏、三屏和显示器热插拔已验证。
- [ ] HDR 开/关时不调用未定义的 Gamma 路径；偏好保留、实际抑制原因可见，关闭 HDR 后按最新意图恢复。
- [ ] 多输出 Gamma 任一失败时已修改输出全部回滚；能力未完全验证、Gamma 静默失败、调暗或专注遮罩失败均不显示“已生效”。
- [ ] “恢复原始显示并关闭屏幕效果”关闭色温、调暗和专注偏好，恢复后不会被调度立即重新应用。
- [ ] 浏览器全屏视频、PowerPoint 放映、D3D 游戏、Alt+Tab、应用规则、锁屏、睡眠恢复和显示热插拔均已验证。
- [ ] 情境传感器连续失败 5 秒后清除非安全抑制，锁屏和睡眠仍保持安全暂停。

## 休息与桌面伙伴

- [ ] 20-20-20、番茄钟、平衡节奏和自定义短/长周期全部验证；长周期必须大于短周期。
- [ ] 短/长同时到期时长休息优先；短休息只重置短周期，长休息和自然休息重置两个周期。
- [ ] 空闲 2/5 分钟边界、延后、跳过、严格模式、锁屏、睡眠和系统时间变化全部覆盖。
- [ ] 渐进提示不抢焦点；60 秒升级、现在休息、延后 5/10/30 分钟、跳过、关闭/`Esc` 等同延后 5 分钟及撤销提示均已验证。
- [ ] 全屏与严格提醒始终保留 `Esc` 和明确安全退出；严格模式不显示延后。
- [ ] 托盘一级“显示桌面伙伴”、预览、重置位置均可用，重启恢复相对锚点，越界时回收到主屏右下角。
- [ ] 宠物隐藏或被情境抑制后无活动动画、光标采样或自主行为定时器；系统减少动画、主题切换和 200% DPI 已验证。

## 热键、自动化、隐私与更新

- [ ] 热键使用 Win32 `RegisterHotKey`；整组交换任一注册失败时完整恢复旧映射并显示中文错误。
- [ ] 固定时间与日出日落均正确应用星期规则；日间/夜间方案和 `-120` 到 `+120` 分钟偏移已覆盖跨日边界。
- [ ] 未点击“检查更新”且未授权天气时零网络请求；更新离线/超时/GitHub 限流/无效 JSON/预发布版本均有明确结果且不影响核心功能。
- [ ] 更新检查不发送设备标识、不后台下载、不自动安装。
- [ ] 状态、日志、配置备份和诊断 ZIP 不含窗口标题、完整 EXE 路径、坐标 URL、便签正文、鼠标轨迹、天气结果、互动次数或每日/逐应用历史。
- [ ] 应用选择器只把小写 EXE basename 传入 Controller；路径分隔符、盘符、空值、超长标识和超过 100 条规则均被拒绝。

## 性能与稳定性

- [ ] 典型 4 核/8GB Windows 设备启动不超过 3 秒。
- [ ] 稳定空闲 CPU 低于 1%，工作集低于 150MB。
- [ ] 宠物帧动画最高 20 fps；延迟时跳过过期帧，光标采样在远离/靠近时为 2/10 Hz，减少动画与节电状态使用静态最终帧。
- [ ] 禁止移动、伙伴隐藏、拖动、全屏/锁屏/睡眠或休息接管时，窗口避让、动画、光标和非必要系统指标采样停止。
- [ ] 8 小时运行无遮罩闪现、漏恢复、恢复旧设置或宠物动画/采样定时器泄漏。

## GitHub 发布物

- [ ] `pyproject.toml` 版本为 `0.6.1`，Tag 为 `v0.6.1`。
- [ ] `dist\OpenCareEyes.exe` 可在干净系统上启动，第二次启动能唤起现有实例。
- [ ] `OpenCareEyes_Setup_0.6.1.exe` 可安装、从 v0.5.0 升级及卸载，AppId、应用名和用户数据升级路径保持不变。
- [ ] `SHA256SUMS.txt` 与便携版、安装版和 WinGet 清单压缩包匹配。
- [ ] 便携版与安装版均包含 `THIRD_PARTY_NOTICES.md` 和 `licenses/`，Release notice 同时纳入校验值。
- [ ] README 的白鼬预览与发行包资源一致；v0.4 历史 GIF 明确标为旧界面，任何 v0.6 截图都来自最终 Qt 构建。
- [ ] README、PRODUCT、DESIGN、使用说明、隐私说明、医疗边界与 Release Notes 均和真实实现一致。
- [ ] GitHub Release 资产 URL 已固定且可匿名下载，Release Notes 来自 `CHANGELOG.md` 与真实提交。

## WinGet（GitHub Release 完成后）

- [ ] 使用最终安装包运行 `python scripts\generate_winget_manifest.py .\installer_output\OpenCareEyes_Setup_0.6.1.exe --version 0.6.1`。
- [ ] 清单中的安装包 URL 指向已发布且版本固定的 `v0.6.1` 资产，SHA-256 与在线资产完全一致。
- [ ] 对 `winget_output\manifests\o\Odyphus\OpenCareEyes\0.6.1` 执行 `winget validate` 并通过。
- [ ] 在全新 Windows Sandbox 中验证静默安装、启动、升级和卸载；无残留自启动项，用户数据保留/清理行为符合说明。
- [ ] 完成上述验证后再向 `microsoft/winget-pkgs` 提交；未合并前不得声称已经被 WinGet 官方源收录。
- [ ] Release Notes 与 README 明确：WinGet、SHA-256 不等同于代码签名，也不能保证消除 SmartScreen。
